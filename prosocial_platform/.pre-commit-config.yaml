# Hooks live in the repo root `.pre-commit-config.yaml`.
# Install from the repository root:
#
#   pip install pre-commit
#   pre-commit install --hook-type pre-commit --hook-type pre-push
#
# Or run: scripts/install-git-hooks.ps1 (Windows) / scripts/install-git-hooks.sh
